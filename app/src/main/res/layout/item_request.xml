<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="12dp"
    android:background="@drawable/bg_card"
    android:layout_marginBottom="8dp">

    <!-- HEADER + STATUS -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:elevation="4dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvRequestType"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Gas Payment"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#172747" />

        <TextView
            android:id="@+id/tvRequestStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Pending"
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="#FFA500" />
    </LinearLayout>

    <!-- AMOUNT -->
    <TextView
        android:id="@+id/tvRequestAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Amount: ₱500"
        android:textSize="14sp"
        android:textColor="#555555"
        android:paddingTop="6dp" />

    <!-- SUBMIT DATE -->
    <TextView
        android:id="@+id/tvRequestTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Submitted: Feb 12, 2026 14:30"
        android:textSize="12sp"
        android:textColor="#888888"
        android:paddingTop="2dp" />

    <!-- ADMIN REPLY IMAGE -->
    <ImageView
        android:id="@+id/ivAdminReply"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:scaleType="centerCrop"
        android:background="#EEEEEE"
        android:visibility="gone"
        android:contentDescription="Admin reply image" />

</LinearLayout>
